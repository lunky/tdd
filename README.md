This is a decent start for js tdd

Clone it, 
`npm i`

Install gulp cli,
`npm i -g gulp`

Then run with `gulp test` which will run tests in ./tests/**/*

... or even better ...

Run with `gulp tdd` and it will run and *watch* for tests in ./tests/**/*

* TDD ALL THE THINGS *:watch